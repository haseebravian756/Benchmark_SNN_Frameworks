# Norse with SuperSpike -- the surrogate that looks like the obvious choice.
#
# Purpose: a controlled comparison against the default config's circ(0.5), to
# quantify how much the SURROGATE CHOICE ALONE moves accuracy, spike rate and
# training dynamics. Everything else is inherited from default.yaml, so the two
# runs differ in exactly one setting and cannot drift apart.
#
#   python train.py --config config/norse_super.yaml --framework norse
#
# Expect: gradients roughly 6x larger than the ATan-based frameworks, hence an
# effectively larger learning rate at the same lr value. See
# docs/norse_superspike_alpha_finding.md.
#
# NOTE: `alpha` below has NO EFFECT in norse 1.1.0 -- its SuperSpike backward
# ignores it. 10.0 is written only because it is the value most guides suggest
# as "closest to ATan"; the behaviour is identical at any value.

extends: default.yaml

# Its own experiment folder. Without this it would inherit ex1's paths and mix
# these rows into ex1's CSV -- run_id and config_hash would still tell them
# apart, but the folder would no longer be a clean record of one experiment.
metrics:
  results_dir: experiments/ex6_norse_surrogate/results

equivalence:
  output_dir: experiments/ex6_norse_surrogate/equivalence

neuron:
  norse:
    surrogate:
      type: super
      alpha: 10.0
